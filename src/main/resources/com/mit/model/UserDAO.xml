<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.mit.model.UserDAO">
	<resultMap type="com.mit.model.UserDTO" id="userMap">
		<id property="username" column="empl_num" />
		<result property="password" column="empl_password" />
		<collection property="authList" resultMap="authMap">
		</collection>
	</resultMap>
	
	<resultMap type="com.mit.model.AuthVO" id="authMap">
		<result property="username" column="empl_num" />
		<result property="auth" column="authority" />
	</resultMap>

	<select id="getUserById" resultMap="userMap">
		SELECT empl_num, empl_password, authority FROM employee WHERE empl_num=#{ userid }
	</select>
	
	<insert id="doRegistUser">
		<foreach collection="authList" item="authVO">
			INSERT INTO member(member_id, member_pw, authority) VALUE ( #{ username }, #{ password }, #{ authVO.auth } )
		</foreach>
	</insert>
	<insert id="doRegistEmpl">
		INSERT INTO employee(dept_num, member_id, email) VALUE ( #{ deptNum }, #{ username }, #{ email } )
	</insert>
	<update id="updatePasswordByEmail">
		UPDATE member m SET member_pw = #{ password }
		WHERE member_id = 
			(SELECT member_id FROM employee AS e
				WHERE e.email = #{ email })
	</update>
</mapper>